[pytest]
testpaths = tests
markers =
    smoke: minimum launch and health checks
    core: first-version core regression suite
    convert: video format conversion tests
    compress: video compression tests
    audio: audio extraction tests
    gif: video to GIF tests
